import React from 'react';
import UserSettings from '../components/UserSettings';

const Settings = () => {
    return (
        <div>
            {/* <h1>Settings</h1> */}
            <UserSettings />
        </div>
    );
};

export default Settings;
