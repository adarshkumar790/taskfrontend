import React from 'react';
import TaskList from '../components/TaskList';

const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            <TaskList />
        </div>
    );
};

export default Dashboard;
